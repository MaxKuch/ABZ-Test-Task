import React from 'react'
const DrawerToggleButton = ({ drawerClickHandler }) => (
    <button onClick = {drawerClickHandler} className="drawer-toggle-button">
    </button>
)

export default DrawerToggleButton